extends: default

rules:
  # Long lines common in workflow files — disable length check
  line-length: disable
  # Allow both true/false and on/off for YAML booleans
  truthy:
    allowed-values: ["true", "false", "on", "off"]
    check-keys: false
