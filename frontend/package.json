{
  "name": "step-ca-ui-frontend",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Phase 0 spike: boots, calls getSession through the generated client, renders the session state discriminator. Throwaway quality. The client package is deliberately absent from dependencies: it is installed with `npm install --no-save` after `npm ci` (D8).",
  "license": "SEE LICENSE IN ../LICENSE",
  "scripts": {
    "dev": "vite",
    "build": "tsc -b && vite build",
    "typecheck": "tsc --noEmit",
    "preview": "vite preview"
  },
  "dependencies": {
    "react": "19.2.0",
    "react-dom": "19.2.0"
  },
  "devDependencies": {
    "@types/react": "19.2.2",
    "@types/react-dom": "19.2.1",
    "@vitejs/plugin-react": "6.1.1",
    "typescript": "5.9.3",
    "vite": "8.2.2"
  }
}
