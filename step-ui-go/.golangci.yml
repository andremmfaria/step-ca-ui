version: "2"

linters:
  default: none
  enable:
    # Original set (kept)
    - errcheck
    - govet
    - staticcheck
    - ineffassign
    - unused
    - misspell
    # Added in PR-0
    - revive
    - gocritic
    - errorlint
    - bodyclose
    - noctx
    - gosec
    - unconvert
    - unparam
    # Added in PR-23 now that log/slog is adopted
    - sloglint

  settings:
    gocritic:
      enabled-tags:
        - diagnostic
        - performance
        - style
      disabled-tags:
        - opinionated
        - experimental
    gosec:
      excludes:
        # errcheck already covers unchecked errors (G104)
        - G104

  exclusions:
    paths:
      # node_modules installed locally for frontend linters — not Go source
      - node_modules/
    rules:
      # Suppress deprecated-symbol warnings on existing code — burn down in PR-23
      - linters: [staticcheck]
        text: "SA1019:"
      # Suppress style/quickfix staticcheck hints in existing code
      - linters: [staticcheck]
        text: "^QF"
      - linters: [staticcheck]
        text: "ST1005:"
      - linters: [staticcheck]
        text: "S1039:"

formatters:
  enable:
    - gofumpt
  settings:
    gofumpt:
      # module-path tells gofumpt which import prefix is "local" so it can
      # produce the three-group layout (stdlib | external | local).
      module-path: step-ui

issues:
  # Ratchet baseline advanced in PR-23.  The previous frozen SHA (a3ba460) had
  # the full legacy backlog; this SHA freezes the post-PR-23 residual (82 issues:
  # 50 revive exported-comment, 13 gosec pre-existing, 12 noctx pre-existing,
  # 6 gocritic style, 1 unparam).  Each future PR must clean up its own diff;
  # the residual backlog is tracked for Wave 4.
  new-from-rev: a119977
  # Caps removed — the ratchet baseline handles scope.
